export function EmptyState({ title }: { title: string }) {
  return <div className="rounded-lg border border-dashed border-line bg-slate-50/80 p-8 text-center text-sm font-medium text-muted">{title}</div>;
}
